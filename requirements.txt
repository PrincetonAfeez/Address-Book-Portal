# Production dependencies (Railway, minimal installs).
# Local development with tests: pip install -e ".[dev]"
Django>=5.0,<6.0
Pillow>=10.0
psycopg[binary]>=3.1
python-dotenv>=1.0
gunicorn>=22.0
whitenoise>=6.6
